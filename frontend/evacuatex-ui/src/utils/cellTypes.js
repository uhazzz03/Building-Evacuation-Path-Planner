// Grid cell values. Must stay in sync with the native constants in
// evacuatex_native.cpp — the wire format is just a raw byte per cell.
export const CELL_OPEN = 0;
export const CELL_WALL = 1;
export const CELL_DOOR = 2;
export const CELL_CORRIDOR = 3;
